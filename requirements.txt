# =============================================================================
# Google Machine Learning Crash Course — Unified Requirements
# =============================================================================
# Compatible with Python 3.10+
# Install with: pip install -r requirements.txt
#
# NOTE — TensorFlow version conflict:
#   Exercises 01 & 02 use tensorflow~=2.18.0
#   Exercise  04 uses tensorflow==2.15 (pinned by fairness-indicators==0.46.0)
#
#   Recommended approach:
#     - Use a separate virtual environment for Exercise 04, OR
#     - Run Exercise 04 in Google Colab (uses its own !pip install cell)
#     - All other exercises (01, 02, 03, 05) share a compatible environment
# =============================================================================


# ── Core Data Science ─────────────────────────────────────────────────────────
# Used across: Ex01, Ex02, Ex03, Ex04, Ex05

numpy~=2.0.0
pandas~=2.2.0


# ── Visualisation ─────────────────────────────────────────────────────────────
# Used across: Ex01, Ex02, Ex03

matplotlib~=3.10.0
plotly>=5.0.0


# ── Deep Learning — TensorFlow / Keras ────────────────────────────────────────
# Used across: Ex01, Ex02, Ex04
# See version conflict note above before installing

tensorflow~=2.18.0          # Ex01, Ex02
keras~=3.8.0                # Ex01, Ex02


# ── Google ML Edu Helper Library ──────────────────────────────────────────────
# Provides ml_edu.experiment and ml_edu.results utilities
# Used across: Ex01, Ex02

google-ml-edu==0.1.3


# ── Fairness & Model Analysis ─────────────────────────────────────────────────
# Used in: Ex04 — Fairness & Bias (Income Prediction)
# Requires tensorflow==2.15 — install in a separate environment

# tensorflow==2.15
# tensorflow-model-analysis==0.46.0
# tensorflow-model-remediation
# fairness-indicators==0.46.0
# tensorflow-data-validation==1.15.1
# protobuf


# ── Decision Forests ──────────────────────────────────────────────────────────
# Used in: Ex05 — Decision Tree & Palmer Penguins Classification

ydf


# ── Jupyter / Notebook Support ────────────────────────────────────────────────

notebook
ipywidgets
